server:
  port: 8080
management:
  security:
    enabled: false #关掉安全认证
  context-path: /monitor #actuator的访问路径
endpoints:
  shutdown:
    enabled: true

info:
   app:
      name: spring-boot-actuator
      version: 1.0.0